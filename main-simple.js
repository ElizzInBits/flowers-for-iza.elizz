// Script simples para inicializar a animação das flores
document.addEventListener('DOMContentLoaded', function() {
    // Remove a classe container para iniciar as animações
    document.body.classList.remove('container');
});